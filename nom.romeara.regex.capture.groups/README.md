# Regex Capture Groups Example

Provides an example of selectively capturing content when performing a regex search on text

### Examples in this Application

* Writing regex patterns which selectively capture content
* Accessing files on the class-path of the application in a way which still works once packaged into a jar archive